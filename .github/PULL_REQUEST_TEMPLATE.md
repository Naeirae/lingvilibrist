## Summary

Describe the product behavior changed by this PR.

## Validation

- [ ] `npm test` passes for JavaScript changes.
- [ ] `npm run build:extension` passes for browser changes.
- [ ] Python NLP tests pass when `python/nlp` changes.
- [ ] A rule that can alter text has positive and negative regression coverage.
- [ ] New context-dependent findings are `review` unless deterministic safety is demonstrated.
- [ ] No private documents, workplace-specific rules, credentials, production URLs, or user data are included.
- [ ] User-facing capability claims match what is actually implemented and tested.
